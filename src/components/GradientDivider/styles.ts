import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    marginTop: 8,
    marginBottom: 20,
  },
  gradient: {
    height: 2,
    width: "100%",
    borderRadius: 1,
  },
});
