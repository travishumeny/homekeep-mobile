import { StyleSheet } from "react-native";

// Styles for the logo section
export const styles = StyleSheet.create({
  logoContainer: {
    marginTop: 80,
    marginBottom: 40,
    alignItems: "center",
  },
  logo: {
    width: 240,
    height: 120,
    resizeMode: "contain",
  },
});
