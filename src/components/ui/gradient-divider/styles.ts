import { StyleSheet } from "react-native";

// styles for the gradient divider
export const styles = StyleSheet.create({
  container: {
    width: "100%",
    paddingHorizontal: 32,
    marginTop: 0,
    marginBottom: 20,
  },
  gradient: {
    height: 2,
    width: "100%",
    borderRadius: 1,
  },
});
